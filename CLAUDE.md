# Career Atlas

Read and follow `AGENTS.md`. It contains the same workflow for Claude and Codex.
Use `docs/AGENT-START.md` to onboard, resume and sync, or handle a change request. It includes source checkpoints and spreadsheet verification.
Start a new installation with `docs/ONBOARDING.md`. Select the relevant skill in `skills/` and resume its saved session before asking the user to repeat information. Personal records stay in the selected data directory; this repository contains software and portable instructions.
